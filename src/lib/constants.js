/** 반 번호 (1~12) */
export const CLASS_NUMBERS = Array.from({ length: 12 }, (_, i) => i + 1);

/** 조 번호 (1~5) */
export const GROUP_NUMBERS = [1, 2, 3, 4, 5];

/** 영역 짧은 라벨 */
export const DOMAIN_SHORT = {
  1: '1영역',
  2: '2영역',
  3: '3영역'
};
